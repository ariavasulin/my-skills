### Next Steps

All questions in the design document have been resolved. At this point, we can author the implementation plan directly as a native demo `plans/` contract — this pipeline skips the separate structure-outline phase.

To do this, create a new session with the following command:
```text
/plan-author
```
