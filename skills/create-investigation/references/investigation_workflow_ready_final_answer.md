### Next Steps

The investigation brief is workflow-ready. Start workflow authoring in a new session with:

```text
/create-workflow .artifacts/TASKNAME
```

That phase will draft the investigation's stage graph for your review, then launch it only after you approve the workflow document. The investigation run—not this skill—creates the live arc, concludes and verifies the corpus, opens the PR, and posts the defined review record.

After you rule on that reviewed PR, use `create-report` or `infographic` in a separate session to compose the client-facing deliverable.

---

You can view the full brief in the right sidebar.
