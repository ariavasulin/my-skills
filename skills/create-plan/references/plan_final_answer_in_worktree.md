### Next Steps

You're already in a worktree (or workspace setup is disabled), so proceed directly to implementation:

```text
/implement-plan 
```

---

You can view the full artifact in the right sidebar
